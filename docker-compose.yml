services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dwselect-app
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
      - NUXT_MODE=${NUXT_MODE:-dev}
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.nuxt
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dwselect.rule=Host(`${APP_URL:?APP_URL 未設定，請在 .env 設定}`)"
      - "traefik.http.routers.dwselect.entrypoints=websecure"
      - "traefik.http.routers.dwselect.tls=true"
      - "traefik.http.services.dwselect.loadbalancer.server.port=3000"
      - "traefik.docker.network=web"
    networks:
      - web

networks:
  web:
    external: true
